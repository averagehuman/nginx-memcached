# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

__version__ = '0.1'


requires = [
]

setup(
        name="instance",
        version=__version__,
        description="",
        download_url="",
        packages = find_packages(),
        install_requires=requires,
)
    

